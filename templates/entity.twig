{% autoescape false %}
<?php
{% if config.namespace %}
namespace {{ config.namespace }};
{% endif %}

/**
 * Class {{ config.namespace }}\{{ entity.name }}
 *
 * This class was generated by a script. Please be careful changing it.
 *
 * @since {{ entity.generated }}
{% if entity.namespace %}
 * @package {{ entity.namespace }}
{% endif %}
 */
class {{ entity.name }} {% if config.extends %}extends \{{ config.extends }}{% endif %}
{
{% for field in entity.fields %}
    {{ field.attribute }}
{% endfor %}
{% for field in entity.fields %}
    {{ field.setter }}
    {{ field.getter }}
{% endfor %}
}
{% endautoescape %}