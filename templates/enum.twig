    if (!is_null(${{ field.name }}) && !in_array(${{ field.name }}, $this->{{ field.name }}Options)) {
            throw new \InvalidArgumentException('Invalid option for {{ field.name }}');
        }