{% autoescape false %}
/**
     * Returns the value of {{ field.name }}
     *
     * @return {{ field.type == 'enum' ? 'string' : field.type }}
     */
    public function get{{ field.ucName }}()
    {
        return $this->{{ field.name }};
    }
    {% if field.type == 'enum' %}
    /**
     * Returns the options for field {{ field.name }}
     *
     * @return array
     */
    public function get{{ field.ucName }}Options()
    {
        return $this->{{ field.name }}Options;
    }
    {% endif %}
{% endautoescape %}